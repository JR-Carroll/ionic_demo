import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'new-location-test-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
